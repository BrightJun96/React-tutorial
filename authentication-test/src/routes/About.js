import React from "react";
import { useLocation } from "react-router-dom";

const About = ({ auth }) => {
  return (
    <div>
      <h1>About(Public)</h1>
      <h2>Welcome to awesome Video Streaming! </h2>
    </div>
  );
};

export default About;
