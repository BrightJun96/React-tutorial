login버튼을 눌렀을 때 setAuth가 작동되지않는다.


state값이 전역에서 공유되는 것이 아닌 각 파일에서의 컨텍스트가 있기 때문에 연동되지 않는 것이다.

각 모듈은 독자적인 스코프를 갖는다.

예를 들어, A라는 파일과 B라는 파일이 C라는 모듈에서 x라는 변수를 불러와 쓰고 있다고 하였을 때
A파일에서의 x와 B파일에서의 x는 같지않다.

즉, A파일에서의 x가 변한다하여도 B파일에서의 x가 변하는 것이 아니다.